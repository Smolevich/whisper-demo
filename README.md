# Whisper - Транскрипция аудио

Проект для транскрипции аудио и видео файлов с использованием Whisper. Поддерживает два режима работы: REST API через Docker и веб-приложение в браузере через GitHub Pages.

## 🚀 Два способа использования

### 1. REST API (Docker Compose)

Серверное решение с использованием WhisperX через REST API.

#### Быстрый старт

```bash
docker-compose up -d --build
```

После запуска сервис будет доступен на `http://localhost:8000`

#### Примеры запросов

**Транскрипция аудио файла:**
```bash
curl -X POST "http://localhost:8000/transcribe" \
  -F "audio_file=@/path/to/audio.mp3" \
  -F "language=ru" \
  -F "model=base"
```

**С выравниванием (alignment):**
```bash
curl -X POST "http://localhost:8000/transcribe" \
  -F "audio_file=@/path/to/audio.mp3" \
  -F "language=ru" \
  -F "model=base" \
  -F "align=true"
```

**Проверка здоровья:**
```bash
curl http://localhost:8000/health
```

**Документация API:**
Откройте в браузере `http://localhost:8000/docs`

#### Параметры транскрипции

- `language` - язык аудио (ru, en, и т.д.), по умолчанию "ru"
- `model` - модель Whisper (tiny, base, small, medium, large), по умолчанию "base"
- `align` - включить выравнивание временных меток, по умолчанию false

---

### 2. Веб-приложение в браузере (GitHub Pages)

Клиентское решение, работающее полностью в браузере без сервера, используя Transformers.js и WebAssembly.

#### Возможности

- ✅ Работает полностью в браузере (без сервера)
- ✅ Поддержка drag & drop файлов
- ✅ Выбор модели Whisper (tiny, base, small, medium)
- ✅ Поддержка русского и английского языков
- ✅ Автоматическое определение языка
- ✅ Красивый современный интерфейс

#### Как запустить на GitHub Pages

1. Включите GitHub Pages в настройках репозитория:
   - Settings → Pages
   - Source: GitHub Actions

2. После пуша в main ветку, GitHub Actions автоматически задеплоит приложение

3. Приложение будет доступно по адресу:
   `https://ваш-username.github.io/whisper/`

#### Локальный запуск

Просто откройте `index.html` в браузере или используйте локальный сервер:

```bash
python -m http.server 8000
# или
npx serve .
```

Затем откройте `http://localhost:8000`

#### Технологии

- **Transformers.js** - библиотека для запуска ML моделей в браузере
- **WebAssembly** - для выполнения модели
- **Web Audio API** - для обработки аудио

#### Ограничения браузерной версии

- Модели загружаются из CDN при первом использовании (может занять время)
- Работает только в современных браузерах с поддержкой WebAssembly
- Качество транскрипции зависит от выбранной модели и мощности устройства
- Первая загрузка модели может занять несколько минут
- Рекомендуется использовать модель `base` для баланса скорости и качества

#### Large модели в WebAssembly

Large версии Whisper (`Xenova/whisper-large`) технически могут работать в браузере через WebAssembly, но имеют существенные ограничения:

- ⚠️ **Размер модели**: ~3GB, что требует длительной загрузки и значительного объема памяти
- ⚠️ **Производительность**: Обработка может быть очень медленной на большинстве устройств
- ⚠️ **Память**: Требуется минимум 4-8GB свободной оперативной памяти браузера
- ⚠️ **Стабильность**: Может вызывать зависания браузера на слабых устройствах

**Рекомендации:**
- Для браузерной версии используйте модели `tiny`, `base`, `small` или `medium`
- Large модели лучше использовать через REST API с GPU поддержкой
- Если нужна максимальная точность в браузере, попробуйте `medium` модель

---

## 📁 Структура проекта

```
.
├── api.py              # FastAPI сервер для REST API
├── Dockerfile          # Образ Docker с WhisperX
├── docker-compose.yml  # Конфигурация для запуска сервиса
├── index.html          # Веб-интерфейс для браузера
├── .github/workflows/  # GitHub Actions для деплоя на Pages
├── input/              # Входные аудио файлы (временные)
├── output/             # Результаты транскрипции
└── models/             # Кэш моделей Whisper
```

## 🎯 Модели Whisper

- `tiny` - самая быстрая, минимальная точность
- `base` - быстрая, базовая точность (рекомендуется для браузера)
- `small` - стандартная точность
- `medium` - хороший баланс скорости и точности
- `large` / `large-v3` - максимальная точность, медленнее (только для REST API с GPU)

## 📝 Примечания

- Первый запуск с новой моделью требует времени на загрузку (~3GB для large)
- GPU значительно ускоряет обработку в REST API (требуется nvidia-docker)
- Качество транскрипции зависит от качества аудио и выбранной модели
- Для браузерной версии рекомендуется использовать модели до `medium` включительно
